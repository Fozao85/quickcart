{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd product-service && composer install --no-dev --optimize-autoloader && cp .env.example .env && php artisan key:generate --force"
  },
  "deploy": {
    "startCommand": "cd product-service && ./deploy.sh",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300
  }
}
